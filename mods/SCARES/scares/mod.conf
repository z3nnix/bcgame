name = scares
description = Event-driven framework for ambient scare events. Mods register scary "events" with lifecycle hooks (on_spawn/on_update/on_finish) and the scheduler picks them periodically for each player.
depends = mcl_util
